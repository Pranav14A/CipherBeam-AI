# AI Datasets

Not yet implemented. Will hold pulse/signal datasets for training the
advisory pulse-classification model (Phase 16), once the deterministic
optical pipeline exists to generate them from.
