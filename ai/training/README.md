# AI Training

Not yet implemented. Will hold model training scripts (Phase 16). AI
remains advisory-only per the architecture decision document — it must
never bypass CRC or AEAD authentication, and a deterministic fallback must
always exist.
