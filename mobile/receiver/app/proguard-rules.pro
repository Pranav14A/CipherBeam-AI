# Add project specific ProGuard rules here.
# Release builds currently have minification disabled (see app/build.gradle.kts),
# so this file has no effect yet — kept as a placeholder for when it's enabled.
