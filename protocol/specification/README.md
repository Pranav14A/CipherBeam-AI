# Protocol Specification

Not yet written.

The approved architectural decisions (Manchester encoding, packet field
list, nonce construction, algorithm selection, AAD scope) are documented in
`docs/architecture/architecture-decision-document.md`. The formal,
implementation-ready protocol specification — with finalized byte widths —
will be written here in Phase 9.
