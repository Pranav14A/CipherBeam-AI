# Crypto Tests

Not yet implemented. Will hold AEAD, nonce-construction, and negative
(tamper/replay/reuse) test cases once the cryptography layer exists
(Phase 11), per the invariants in the architecture decision document.
