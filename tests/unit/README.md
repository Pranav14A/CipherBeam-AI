# Unit Tests (cross-component)

Empty for now. Component-local unit tests (e.g. the backend's
`GET /health` test) live alongside their component
(`desktop/backend/tests/`) per Phase 1 decision. This directory is for
cross-component unit tests once more than one component needs shared
coverage.
